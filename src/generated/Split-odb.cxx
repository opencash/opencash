// This file was generated by ODB, object-relational mapping (ORM)
// compiler for C++.
//

#include <odb/pre.hxx>

#include <generated/Split-odb.hxx>

#include <cassert>
#include <cstring>  // std::memcpy

#include <odb/schema-catalog-impl.hxx>

#include <odb/sqlite/traits.hxx>
#include <odb/sqlite/database.hxx>
#include <odb/sqlite/transaction.hxx>
#include <odb/sqlite/connection.hxx>
#include <odb/sqlite/statement.hxx>
#include <odb/sqlite/statement-cache.hxx>
#include <odb/sqlite/simple-object-statements.hxx>
#include <odb/sqlite/container-statements.hxx>
#include <odb/sqlite/exceptions.hxx>
#include <odb/sqlite/simple-object-result.hxx>

namespace odb
{
  // Split
  //

  const char alias_traits<  ::opencash::model::Transaction,
    id_sqlite,
    access::object_traits_impl< ::opencash::model::Split, id_sqlite >::transaction_tag>::
  table_name[] = "\"transaction\"";

  const char alias_traits<  ::opencash::model::Account,
    id_sqlite,
    access::object_traits_impl< ::opencash::model::Split, id_sqlite >::account_tag>::
  table_name[] = "\"account\"";

  struct access::object_traits_impl< ::opencash::model::Split, id_sqlite >::extra_statement_cache_type
  {
    extra_statement_cache_type (
      sqlite::connection&,
      image_type&,
      sqlite::binding&,
      sqlite::binding&)
    {
    }
  };

  bool access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  grow (image_type& i,
        bool* t)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (t);

    bool grew (false);

    // ManagedObject base
    //
    if (object_traits_impl< ::opencash::model::ManagedObject, id_sqlite >::grow (
          i, t + 0UL))
      grew = true;

    // _transaction
    //
    if (t[1UL])
    {
      i._transaction_value.capacity (i._transaction_size);
      grew = true;
    }

    // _account
    //
    if (t[2UL])
    {
      i._account_value.capacity (i._account_size);
      grew = true;
    }

    // _memo
    //
    if (t[3UL])
    {
      i._memo_value.capacity (i._memo_size);
      grew = true;
    }

    // _action
    //
    if (t[4UL])
    {
      i._action_value.capacity (i._action_size);
      grew = true;
    }

    // _value
    //
    t[5UL] = false;

    return grew;
  }

  void access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  bind (sqlite::bind* b,
        image_type& i,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    std::size_t n (0);

    // ManagedObject base
    //
    object_traits_impl< ::opencash::model::ManagedObject, id_sqlite >::bind (b + n, i, sk);
    n += sk == statement_update ? 0UL : 1UL;

    // _transaction
    //
    b[n].type = sqlite::image_traits<
      ::opencash::model::ManagedObject::Uuid,
      sqlite::id_text>::bind_value;
    b[n].buffer = i._transaction_value.data ();
    b[n].size = &i._transaction_size;
    b[n].capacity = i._transaction_value.capacity ();
    b[n].is_null = &i._transaction_null;
    n++;

    // _account
    //
    b[n].type = sqlite::image_traits<
      ::opencash::model::ManagedObject::Uuid,
      sqlite::id_text>::bind_value;
    b[n].buffer = i._account_value.data ();
    b[n].size = &i._account_size;
    b[n].capacity = i._account_value.capacity ();
    b[n].is_null = &i._account_null;
    n++;

    // _memo
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i._memo_value.data ();
    b[n].size = &i._memo_size;
    b[n].capacity = i._memo_value.capacity ();
    b[n].is_null = &i._memo_null;
    n++;

    // _action
    //
    b[n].type = sqlite::image_traits<
      ::std::string,
      sqlite::id_text>::bind_value;
    b[n].buffer = i._action_value.data ();
    b[n].size = &i._action_size;
    b[n].capacity = i._action_value.capacity ();
    b[n].is_null = &i._action_null;
    n++;

    // _value
    //
    b[n].type = sqlite::bind::real;
    b[n].buffer = &i._value_value;
    b[n].is_null = &i._value_null;
    n++;
  }

  bool access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  init (image_type& i,
        const object_type& o,
        sqlite::statement_kind sk)
  {
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (sk);

    using namespace sqlite;

    bool grew (false);

    // ManagedObject base
    //
    if (object_traits_impl< ::opencash::model::ManagedObject, id_sqlite >::init (i, o, sk))
      grew = true;

    // _transaction
    //
    {
      ::opencash::model::TransactionWeakPtr const& v =
        o._transaction;

      typedef object_traits< ::opencash::model::Transaction > obj_traits;
      typedef odb::pointer_traits< ::opencash::model::TransactionWeakPtr > wptr_traits;
      typedef odb::pointer_traits< wptr_traits::strong_pointer_type > ptr_traits;

      wptr_traits::strong_pointer_type sp (wptr_traits::lock (v));
      bool is_null (ptr_traits::null_ptr (sp));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (sp)));

        std::size_t cap (i._transaction_value.capacity ());
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_image (
          i._transaction_value,
          i._transaction_size,
          is_null,
          id);
        i._transaction_null = is_null;
        grew = grew || (cap != i._transaction_value.capacity ());
      }
      else
        throw null_pointer ();
    }

    // _account
    //
    {
      ::opencash::model::AccountWeakPtr const& v =
        o._account;

      typedef object_traits< ::opencash::model::Account > obj_traits;
      typedef odb::pointer_traits< ::opencash::model::AccountWeakPtr > wptr_traits;
      typedef odb::pointer_traits< wptr_traits::strong_pointer_type > ptr_traits;

      wptr_traits::strong_pointer_type sp (wptr_traits::lock (v));
      bool is_null (ptr_traits::null_ptr (sp));
      if (!is_null)
      {
        const obj_traits::id_type& id (
          obj_traits::id (ptr_traits::get_ref (sp)));

        std::size_t cap (i._account_value.capacity ());
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_image (
          i._account_value,
          i._account_size,
          is_null,
          id);
        i._account_null = is_null;
        grew = grew || (cap != i._account_value.capacity ());
      }
      else
        throw null_pointer ();
    }

    // _memo
    //
    {
      ::std::string const& v =
        o._memo;

      bool is_null (false);
      std::size_t cap (i._memo_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i._memo_value,
        i._memo_size,
        is_null,
        v);
      i._memo_null = is_null;
      grew = grew || (cap != i._memo_value.capacity ());
    }

    // _action
    //
    {
      ::std::string const& v =
        o._action;

      bool is_null (false);
      std::size_t cap (i._action_value.capacity ());
      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_image (
        i._action_value,
        i._action_size,
        is_null,
        v);
      i._action_null = is_null;
      grew = grew || (cap != i._action_value.capacity ());
    }

    // _value
    //
    {
      double const& v =
        o._value;

      bool is_null (false);
      sqlite::value_traits<
          double,
          sqlite::id_real >::set_image (
        i._value_value,
        is_null,
        v);
      i._value_null = is_null;
    }

    return grew;
  }

  void access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  init (object_type& o,
        const image_type& i,
        database* db)
  {
    ODB_POTENTIALLY_UNUSED (o);
    ODB_POTENTIALLY_UNUSED (i);
    ODB_POTENTIALLY_UNUSED (db);

    // ManagedObject base
    //
    object_traits_impl< ::opencash::model::ManagedObject, id_sqlite >::init (o, i, db);

    // _transaction
    //
    {
      ::opencash::model::TransactionWeakPtr& v =
        o._transaction;

      typedef object_traits< ::opencash::model::Transaction > obj_traits;
      typedef odb::pointer_traits< ::opencash::model::TransactionWeakPtr > ptr_traits;

      if (i._transaction_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_value (
          id,
          i._transaction_value,
          i._transaction_size,
          i._transaction_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<sqlite::database*> (db)->load<
            obj_traits::object_type > (id));

        if (odb::pointer_traits<ptr_traits::strong_pointer_type>::null_ptr (
              ptr_traits::lock (v)))
          throw session_required ();
      }
    }

    // _account
    //
    {
      ::opencash::model::AccountWeakPtr& v =
        o._account;

      typedef object_traits< ::opencash::model::Account > obj_traits;
      typedef odb::pointer_traits< ::opencash::model::AccountWeakPtr > ptr_traits;

      if (i._account_null)
        v = ptr_traits::pointer_type ();
      else
      {
        obj_traits::id_type id;
        sqlite::value_traits<
            obj_traits::id_type,
            sqlite::id_text >::set_value (
          id,
          i._account_value,
          i._account_size,
          i._account_null);

        // If a compiler error points to the line below, then
        // it most likely means that a pointer used in a member
        // cannot be initialized from an object pointer.
        //
        v = ptr_traits::pointer_type (
          static_cast<sqlite::database*> (db)->load<
            obj_traits::object_type > (id));

        if (odb::pointer_traits<ptr_traits::strong_pointer_type>::null_ptr (
              ptr_traits::lock (v)))
          throw session_required ();
      }
    }

    // _memo
    //
    {
      // From Split.h:52:18
      ::std::string v;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i._memo_value,
        i._memo_size,
        i._memo_null);

      // From Split.h:52:18
      o.setMemo (v);
    }

    // _action
    //
    {
      // From Split.h:55:18
      ::std::string v;

      sqlite::value_traits<
          ::std::string,
          sqlite::id_text >::set_value (
        v,
        i._action_value,
        i._action_size,
        i._action_null);

      // From Split.h:55:18
      o.setAction (v);
    }

    // _value
    //
    {
      // From Split.h:58:27
      double v;

      sqlite::value_traits<
          double,
          sqlite::id_real >::set_value (
        v,
        i._value_value,
        i._value_null);

      // From Split.h:58:27
      o.setValue (v);
    }
  }

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::persist_statement[] =
  "INSERT INTO \"splits\" "
  "(\"uuid\", "
  "\"transaction\", "
  "\"account\", "
  "\"memo\", "
  "\"action\", "
  "\"value\") "
  "VALUES "
  "(?, ?, ?, ?, ?, ?)";

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::find_statement[] =
  "SELECT "
  "\"splits\".\"uuid\", "
  "\"splits\".\"transaction\", "
  "\"splits\".\"account\", "
  "\"splits\".\"memo\", "
  "\"splits\".\"action\", "
  "\"splits\".\"value\" "
  "FROM \"splits\" "
  "WHERE \"splits\".\"uuid\"=?";

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::update_statement[] =
  "UPDATE \"splits\" "
  "SET "
  "\"transaction\"=?, "
  "\"account\"=?, "
  "\"memo\"=?, "
  "\"action\"=?, "
  "\"value\"=? "
  "WHERE \"uuid\"=?";

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::erase_statement[] =
  "DELETE FROM \"splits\" "
  "WHERE \"uuid\"=?";

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::query_statement[] =
  "SELECT\n"
  "\"splits\".\"uuid\",\n"
  "\"splits\".\"transaction\",\n"
  "\"splits\".\"account\",\n"
  "\"splits\".\"memo\",\n"
  "\"splits\".\"action\",\n"
  "\"splits\".\"value\"\n"
  "FROM \"splits\"\n"
  "LEFT JOIN \"transactions\" AS \"transaction\" ON \"transaction\".\"uuid\"=\"splits\".\"transaction\"\n"
  "LEFT JOIN \"accounts\" AS \"account\" ON \"account\".\"uuid\"=\"splits\".\"account\"";

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::erase_query_statement[] =
  "DELETE FROM \"splits\"";

  const char access::object_traits_impl< ::opencash::model::Split, id_sqlite >::table_name[] =
  "\"splits\"";

  void access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  persist (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    callback (db,
              obj,
              callback_event::pre_persist);

    image_type& im (sts.image ());
    binding& imb (sts.insert_image_binding ());

    if (init (im, obj, statement_insert))
      im.version++;

    if (im.version != sts.insert_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_insert);
      sts.insert_image_version (im.version);
      imb.version++;
    }

    insert_statement& st (sts.persist_statement ());
    if (!st.execute ())
      throw object_already_persistent ();

    callback (db,
              obj,
              callback_event::post_persist);
  }

  void access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  update (database& db, const object_type& obj)
  {
    ODB_POTENTIALLY_UNUSED (db);

    using namespace sqlite;
    using sqlite::update_statement;

    callback (db, obj, callback_event::pre_update);

    sqlite::transaction& tr (sqlite::transaction::current ());
    sqlite::connection& conn (tr.connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    const id_type& id (
      obj._uuid);
    id_image_type& idi (sts.id_image ());
    init (idi, id);

    image_type& im (sts.image ());
    if (init (im, obj, statement_update))
      im.version++;

    bool u (false);
    binding& imb (sts.update_image_binding ());
    if (im.version != sts.update_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_update);
      sts.update_image_version (im.version);
      imb.version++;
      u = true;
    }

    binding& idb (sts.id_image_binding ());
    if (idi.version != sts.update_id_image_version () ||
        idb.version == 0)
    {
      if (idi.version != sts.id_image_version () ||
          idb.version == 0)
      {
        bind (idb.bind, idi);
        sts.id_image_version (idi.version);
        idb.version++;
      }

      sts.update_id_image_version (idi.version);

      if (!u)
        imb.version++;
    }

    update_statement& st (sts.update_statement ());
    if (st.execute () == 0)
      throw object_not_persistent ();

    callback (db, obj, callback_event::post_update);
    pointer_cache_traits::update (db, obj);
  }

  void access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  erase (database& db, const id_type& id)
  {
    using namespace sqlite;

    ODB_POTENTIALLY_UNUSED (db);

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    id_image_type& i (sts.id_image ());
    init (i, id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    if (sts.erase_statement ().execute () != 1)
      throw object_not_persistent ();

    pointer_cache_traits::erase (db, id);
  }

  access::object_traits_impl< ::opencash::model::Split, id_sqlite >::pointer_type
  access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  find (database& db, const id_type& id)
  {
    using namespace sqlite;

    {
      pointer_type p (pointer_cache_traits::find (db, id));

      if (!pointer_traits::null_ptr (p))
        return p;
    }

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (l.locked ())
    {
      if (!find_ (sts, &id))
        return pointer_type ();
    }

    pointer_type p (
      access::object_factory<object_type, pointer_type>::create ());
    pointer_traits::guard pg (p);

    pointer_cache_traits::insert_guard ig (
      pointer_cache_traits::insert (db, id, p));

    object_type& obj (pointer_traits::get_ref (p));

    if (l.locked ())
    {
      select_statement& st (sts.find_statement ());
      ODB_POTENTIALLY_UNUSED (st);

      callback (db, obj, callback_event::pre_load);
      init (obj, sts.image (), &db);
      load_ (sts, obj, false);
      sts.load_delayed (0);
      l.unlock ();
      callback (db, obj, callback_event::post_load);
      pointer_cache_traits::load (ig.position ());
    }
    else
      sts.delay_load (id, obj, ig.position ());

    ig.release ();
    pg.release ();
    return p;
  }

  bool access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  find (database& db, const id_type& id, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    reference_cache_traits::position_type pos (
      reference_cache_traits::insert (db, id, obj));
    reference_cache_traits::insert_guard ig (pos);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, false);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    reference_cache_traits::load (pos);
    ig.release ();
    return true;
  }

  bool access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  reload (database& db, object_type& obj)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());
    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    statements_type::auto_lock l (sts);

    const id_type& id  (
      obj._uuid);

    if (!find_ (sts, &id))
      return false;

    select_statement& st (sts.find_statement ());
    ODB_POTENTIALLY_UNUSED (st);

    callback (db, obj, callback_event::pre_load);
    init (obj, sts.image (), &db);
    load_ (sts, obj, true);
    sts.load_delayed (0);
    l.unlock ();
    callback (db, obj, callback_event::post_load);
    return true;
  }

  bool access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  find_ (statements_type& sts,
         const id_type* id)
  {
    using namespace sqlite;

    id_image_type& i (sts.id_image ());
    init (i, *id);

    binding& idb (sts.id_image_binding ());
    if (i.version != sts.id_image_version () || idb.version == 0)
    {
      bind (idb.bind, i);
      sts.id_image_version (i.version);
      idb.version++;
    }

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    select_statement& st (sts.find_statement ());

    st.execute ();
    auto_result ar (st);
    select_statement::result r (st.fetch ());

    if (r == select_statement::truncated)
    {
      if (grow (im, sts.select_image_truncated ()))
        im.version++;

      if (im.version != sts.select_image_version ())
      {
        bind (imb.bind, im, statement_select);
        sts.select_image_version (im.version);
        imb.version++;
        st.refetch ();
      }
    }

    return r != select_statement::no_data;
  }

  result< access::object_traits_impl< ::opencash::model::Split, id_sqlite >::object_type >
  access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  query (database&, const query_base_type& q)
  {
    using namespace sqlite;
    using odb::details::shared;
    using odb::details::shared_ptr;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    statements_type& sts (
      conn.statement_cache ().find_object<object_type> ());

    image_type& im (sts.image ());
    binding& imb (sts.select_image_binding ());

    if (im.version != sts.select_image_version () ||
        imb.version == 0)
    {
      bind (imb.bind, im, statement_select);
      sts.select_image_version (im.version);
      imb.version++;
    }

    std::string text (query_statement);
    if (!q.empty ())
    {
      text += "\n";
      text += q.clause ();
    }

    q.init_parameters ();
    shared_ptr<select_statement> st (
      new (shared) select_statement (
        conn,
        text,
        true,
        true,
        q.parameters_binding (),
        imb));

    st->execute ();

    shared_ptr< odb::object_result_impl<object_type> > r (
      new (shared) sqlite::object_result_impl<object_type> (
        q, st, sts, 0));

    return result<object_type> (r);
  }

  unsigned long long access::object_traits_impl< ::opencash::model::Split, id_sqlite >::
  erase_query (database&, const query_base_type& q)
  {
    using namespace sqlite;

    sqlite::connection& conn (
      sqlite::transaction::current ().connection ());

    std::string text (erase_query_statement);
    if (!q.empty ())
    {
      text += ' ';
      text += q.clause ();
    }

    q.init_parameters ();
    delete_statement st (
      conn,
      text,
      q.parameters_binding ());

    return st.execute ();
  }
}

namespace odb
{
  static bool
  create_schema (database& db, unsigned short pass, bool drop)
  {
    ODB_POTENTIALLY_UNUSED (db);
    ODB_POTENTIALLY_UNUSED (pass);
    ODB_POTENTIALLY_UNUSED (drop);

    if (drop)
    {
      switch (pass)
      {
        case 1:
        {
          return true;
        }
        case 2:
        {
          db.execute ("DROP TABLE IF EXISTS \"splits\"");
          return false;
        }
      }
    }
    else
    {
      switch (pass)
      {
        case 1:
        {
          db.execute ("CREATE TABLE \"splits\" (\n"
                      "  \"uuid\" TEXT NOT NULL PRIMARY KEY,\n"
                      "  \"transaction\" TEXT NOT NULL,\n"
                      "  \"account\" TEXT NOT NULL,\n"
                      "  \"memo\" TEXT NOT NULL,\n"
                      "  \"action\" TEXT NOT NULL,\n"
                      "  \"value\" REAL NOT NULL,\n"
                      "  CONSTRAINT \"transaction_fk\"\n"
                      "    FOREIGN KEY (\"transaction\")\n"
                      "    REFERENCES \"transactions\" (\"uuid\")\n"
                      "    DEFERRABLE INITIALLY DEFERRED,\n"
                      "  CONSTRAINT \"account_fk\"\n"
                      "    FOREIGN KEY (\"account\")\n"
                      "    REFERENCES \"accounts\" (\"uuid\")\n"
                      "    DEFERRABLE INITIALLY DEFERRED)");
          return false;
        }
      }
    }

    return false;
  }

  static const schema_catalog_create_entry
  create_schema_entry_ (
    id_sqlite,
    "",
    &create_schema);
}

#include <odb/post.hxx>
